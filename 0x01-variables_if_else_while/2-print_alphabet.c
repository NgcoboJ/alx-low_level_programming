#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Print the alphabet in lowercase
 * Return: Lowercase
 */

int main(void)

{
	char c;

	for (c = 'a' ; c <= 'z' ; c++)
	putchar(c);
	putchar('\n');
	return (0);

}
